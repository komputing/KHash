rootProject.name = 'khash'
enableFeaturePreview('GRADLE_METADATA')

def includeEx(String name) {
    include name
    project(name).projectDir = "$rootDir${name.replace(':', '/')}" as File
}

includeEx ':extensions'
includeEx ':keccak'
includeEx ':sha3'
includeEx ':ripemd160'
